//! Build script for angr native library.
//!
//! This is a simple build script that doesn't require any external dependencies.

fn main() {
    // No external library configuration needed.
    // The Rust extension uses pure Rust implementations.
}
